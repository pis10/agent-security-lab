# Security Policy / 安全边界声明

本项目是一个**故意脆弱**的 AI/Agent 安全训练靶场。仓库内的漏洞（提示注入、命令注入、SSRF、IDOR、MCP 投毒等）是**教学功能，不是缺陷**。

## 使用边界

- 仅在本地或你明确授权的环境中运行；所有秘密均为 `TEST_*` dummy 数据。
- 所有 mock 端点只绑定 `127.0.0.1`；不要把本服务暴露到局域网/公网——它不具备多租户隔离与认证能力。
- 建议通过 Docker 运行，把故意脆弱的 shell 工具与宿主机隔离。

## 报告问题

如果你发现的问题**超出设计范围**（例如：靶场把宿主机真实文件/真实凭据暴露给攻击链、mock 端点可被外部触达、`.env` 被误打包进镜像），请私下联系项目维护者，不要公开披露细节。
